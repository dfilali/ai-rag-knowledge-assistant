version: '3.8'

services:
  assistant:
    build: .
    container_name: ai-rag-knowledge-assistant
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
